[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Cache Next.js + pnpm
[build.processing]
  skip_processing = false


[[build.cache]]
  paths = [
    ".next/cache",
    "node_modules/.pnpm"
  ]